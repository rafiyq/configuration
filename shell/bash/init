# If not running interactively, don't do anything
[[ $- != *i* ]] && return

BASH_CONFIG=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &> /dev/null && pwd)

source $BASH_CONFIG/aliases
source $BASH_CONFIG/helper
source $BASH_CONFIG/env
source $BASH_CONFIG/prompt
